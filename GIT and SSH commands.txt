// SSH Commands

ssh-keygen -t rsa -b 4096 -C "nithishkumar277@gmail.com"
eval $(ssh-agent -s)
ssh-add ~/.ssh/id_rsa

// Git Commands

// add gitignore

1. git init
2. git status
3. git add .
4. git status
5. git commit -m "Commiting"
